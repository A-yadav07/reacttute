import React from 'react';

function Custombutton({name}) {
    return (
        <div className='inputcontainer' >
            
            <button className='mybuttom' >{name}</button>
        </div>
        
    );
}

export default Custombutton;
